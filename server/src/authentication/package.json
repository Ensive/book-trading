{
  "name": "authentication",
  "version": "0.0.1",
  "description": "Authentication Microservice",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "dev": "nodemon --watch './**/*.ts' --ignore 'sessions/' --exec 'ts-node' ./index.ts",
    "prod": "npm run build && node ./index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "mongo"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "connect-mongo": "^2.0.3",
    "express": "^4.16.4",
    "express-session": "^1.15.6",
    "mongoose": "^5.4.17",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^1.18.10",
    "ts-node": "^8.0.2",
    "typescript": "^3.3.3333"
  }
}
